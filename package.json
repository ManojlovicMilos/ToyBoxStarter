{
  "name": "tbx-starter",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "start": "concurrently -k \"npm run build\" \"npm run serve\"",
    "build": "tsc && vite build --watch",
    "serve": "vite preview"
  },
  "devDependencies": {
    "concurrently": "9.2.1",
    "sass-embedded": "1.93.2",
    "typescript": "5.9.3",
    "vite": "7.1.10"
  },
  "dependencies": {
    "toybox-engine": "0.2.3-beta.3"
  }
}
